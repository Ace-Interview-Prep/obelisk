# Jenga libraries

This directory contains libraries provided by Jenga. By convention they are prefixed with `jenga-` and their module exports all lie within the `Jenga` import namespace. For example, to include `frontend` as a dependency you would add `jenga-frontend` to your cabal dependencies, and import `Jenga.Frontend...` in the source files.
